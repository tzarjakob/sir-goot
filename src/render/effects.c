#include <ncurses.h>
#include <stdio.h>
#include "effects.h"

int effect1(WINDOW* win, size_t time)
{
    int retval = 0;
    int WIDTH, HEIGHT;
    getmaxyx(stdscr, HEIGHT, WIDTH);
    

    return retval;
}